rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // プロトタイプ段階: 認証済みユーザーは全操作可
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
